// El builder @angular/build:unit-test (Angular 21+) inicializa TestBed automaticamente.
// Este archivo solo sirve como punto de setup global para configuracion adicional.
import '@angular/compiler';
